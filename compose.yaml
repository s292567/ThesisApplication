services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=postgres'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
  keycloak:
    image: 's292567/keycloak'
    container_name: keycloak
    ports:
      - "8080:8080"
    #    environment:
    #      - KEYCLOAK_ADMIN_PASSWORD=admin
    #      - KEYCLOAK_ADMIN=admin
    command: start-dev
